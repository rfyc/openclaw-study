# 安全策略

如果您认为在 OpenClaw 中发现了安全问题，请先私下报告。

此策略做两件事：为研究人员提供清晰的披露路径，并阐明维护者在分类报告时使用的信任模型。OpenClaw 是面向受信任操作员的本地优先代理基础设施；它不设计为单个网关上对抗性用户之间的共享多租户边界。

最快有用的报告会展示当前可重现的边界绕过并具有已证明的影响。扫描仪输出、仅提示注入链，或依赖在一个受信任网关上共享的恶意用户的报告，在此模型下通常不是安全漏洞。

安全工作由多位 OpenClaw 维护者共享，包括来自 NVIDIA 和腾讯等组织的工程师和安全研究人员。参见[维护者列表](CONTRIBUTING.md#maintainers)。

## 报告安全问题

直接向问题所在的仓库报告漏洞：

- **核心 CLI 和网关** — [openclaw/openclaw](https://github.com/openclaw/openclaw)
- **macOS 桌面应用** — [openclaw/openclaw](https://github.com/openclaw/openclaw)（apps/macos）
- **iOS 应用** — [openclaw/openclaw](https://github.com/openclaw/openclaw)（apps/ios）
- **Android 应用** — [openclaw/openclaw](https://github.com/openclaw/openclaw)（apps/android）
- **ClawHub** — [openclaw/clawhub](https://github.com/openclaw/clawhub)
- **信任和威胁模型** — [openclaw/trust](https://github.com/openclaw/trust)

对于不适合特定仓库的问题，或者如果您不确定，请发送邮件到 **[security@openclaw.ai](mailto:security@openclaw.ai)**，我们会路由它。

对于 OpenClaw 核心问题，通过私密 [GitHub Security Advisory](https://github.com/openclaw/openclaw/security/advisories/new) 提交。不要打开公开 issue 或 PR 来披露未修补的漏洞、利用路径、密钥或对安全敏感的概念验证。

维护者可能关闭、隐藏、删除或以其他方式撤下披露漏洞或活跃安全问题的公开 issue 和 PR。我们将通过私密披露流程重定向这些报告，以便在不给攻击者提供公开操作手册的情况下对问题进行分类和修复。

完整报告说明请参见我们的[信任页面](https://trust.openclaw.ai)。
有关维护者响应工作流，请参见[事件响应计划](docs/security/incident-response.md)。

OpenClaw 目前不运行付费漏洞赏金计划。请仍然负责任地披露，这样我们可以快速修复真实问题。目前帮助项目的最佳方式是发送高信号报告，以及在实际可行时提供有针对性的 PR。

### 我们需要什么

使报告易于重现且易于路由：

- 您发现的内容以及您认为它与安全相关的原因。
- 受影响的组件、版本和尽可能的提交 SHA。
- 针对最新 `main` 或最新发布版本的重现步骤或概念验证。
- 实际影响，包括哪个 OpenClaw 信任边界被跨越。
- 您能提供的任何修复建议或有针对性的补丁。

没有重现步骤、已证明影响和修复建议的报告将被降低优先级。我们收到大量 AI 生成的扫描仪发现，所以我们优先处理来自能够展示问题如何跨越 OpenClaw 安全边界的研究人员的经过验证的报告。

### 通常不是安全 Bug 的情况

这些模式本身通常不是漏洞：

- 没有策略、身份验证、批准、沙盒或工具边界绕过的仅提示注入链。
- 受信任的操作员使用有意的本地功能，例如本地 shell 访问或浏览器/脚本执行。
- 受信任的操作员安装或启用后的恶意插件。
- 多个对抗性用户共享一个网关宿主/配置并期望每用户隔离。
- 仅扫描仪、仅依赖项或过期路径报告，没有工作重现和已证明的 OpenClaw 影响。
- 文档已建议反对的公共互联网暴露或有风险的部署选择。

如果您不确定，请私密报告。我们宁愿路由一份仔细的报告，也不想遗漏一个真实的边界问题。

### 重复报告处理

- 在提交之前搜索现有公告。
- 在您的报告中包含可能的重复 GHSA ID（如适用）。
- 维护者可能会关闭质量较低/较晚的重复，以支持最早的高质量规范报告。

## 安全态势和报告规则

以下部分是维护者用于报告分类的规范态势。标题是编辑性的；策略文本定义边界。

### 详细报告接受门槛

为了最快分类，请包含以下所有内容：

- 当前修订版上的确切易受攻击路径（`file`、函数和行范围）。
- 已测试的版本详情（OpenClaw 版本和/或提交 SHA）。
- 针对最新 `main` 或最新发布版本的可重现 PoC。
- 如果声明针对已发布版本，需要来自已发布标签和该确切版本的已发布产物/包的证据（不仅仅是 `main`）。
- 对于依赖项 CVE 报告，需要已发布依赖版本实际受影响的证据，以及通过 OpenClaw 重现影响的 PoC。仅展示 OpenClaw 可以访问本机解析器是不够的。
- 与 OpenClaw 已记录信任边界相关的已证明影响。
- 对于暴露密钥报告：证明凭证是 OpenClaw 拥有的（或授予对 OpenClaw 运营的基础设施/服务的访问权限）。
- 明确声明该报告不依赖于在一个网关宿主/配置上共享的对抗性操作员。
- 说明为什么该报告**不在**以下超出范围部分的涵盖范围内的范围检查。
- 对于命令风险/对等报告（例如混淆检测差异），需要具体的边界绕过路径（身份验证/批准/允许列表/沙盒）。仅对等发现被视为加固，不是漏洞。

未满足这些要求的报告可能被关闭为 `invalid` 或 `no-action`。

### 详细误报模式

这些经常被报告，但通常在没有代码变更的情况下关闭：

- 没有边界绕过的仅提示注入链（提示注入超出范围）。
- 以远程注入形式呈现的操作员预期本地功能（例如 TUI 本地 `!` shell）。
- 将明确的操作员控制表面（例如 `canvas.eval`、浏览器评估/脚本执行或直接 `node.invoke` 执行原语）作为漏洞的报告，而不展示身份验证/策略/沙盒边界绕过。这些能力在启用时是有意的，是受信任操作员功能，不是独立的安全 bug。
- 将授权的用户触发的本地操作呈现为权限提升。示例：允许列表/所有者发送者运行 `/export-session /absolute/path.html` 以在宿主上写入。在此信任模型中，授权用户操作是受信任宿主操作，除非您证明身份验证/沙盒/边界绕过。
- 仅展示受信任安装/启用的插件可以以网关/宿主权限执行的报告。
- 假设在共享网关宿主/配置上存在每用户多租户授权的报告。
- 仅展示来自非允许列表发送者的引用/回复/线程/转发的补充上下文对模型可见，而不展示身份验证、策略、批准或沙盒边界绕过的报告。
- 将网关 HTTP 兼容端点（`POST /v1/chat/completions`、`POST /v1/responses`）视为实现了有范围的操作员身份验证（`operator.write` 对 `operator.admin`）的报告。这些端点验证共享网关承载密钥/密码，是记录的完全操作员访问表面，不是每用户/每范围边界。
- 假设 `x-openclaw-scopes` 可以减少或重新定义 OpenAI 兼容 HTTP 端点上的共享密钥承载身份验证的报告。对于共享密钥身份验证（`gateway.auth.mode="token"` 或 `"password"`），这些端点忽略更窄的承载声明范围，恢复完整的默认操作员范围集加所有者语义。
- 将共享密钥承载身份验证（`gateway.auth.mode="token"` 或 `"password"`）下的 `POST /tools/invoke` 视为更窄的每请求/每范围授权表面的报告。该端点被设计为相同的受信任操作员 HTTP 边界：共享密钥承载身份验证是该路径的完全操作员访问，更窄的 `x-openclaw-scopes` 值不会减少该路径，所有者专用工具策略遵循共享密钥操作员合约。
- 仅展示启发式检测/对等性差异（例如在一个 exec 路径但不是另一个路径上的混淆模式检测，例如 `node.invoke -> system.run` 对等差距）而不展示身份验证、批准、允许列表执行、沙盒或其他记录信任边界绕过的报告。
- 仅展示 ACP 工具可以间接执行、修改、编排会话或访问另一个工具/运行时，而不展示 ACP 提示/批准、允许列表执行、沙盒或其他记录信任边界绕过的报告。ACP 静默批准有意限于窄只读类；仅对等间接命令发现是加固，不是漏洞。
- 仅展示不受信任的媒体字节到达维护的本机解码器依赖项（例如 Sharp/libvips/libheif），而不证明已发布的依赖版本易受攻击，以及通过 OpenClaw 演示崩溃、内存损坏、数据暴露或边界绕过的报告。JavaScript 头部嗅探和图像尺寸快速路径是预检/UX 检查，不是本机解码器正确性的安全边界。
- 仅在输入已在 OpenClaw 配置的大小/信任限制下被接受后，来自解码、base64 扩展、媒体转码、序列化或其他格式转换的短暂额外内存、CPU 或分配工作的报告，包括 base64 解码前大小估计发现。这些是性能问题，不是漏洞，除非报告证明未经身份验证的放大、配置限制绕过、崩溃/进程终止、持续资源耗尽、数据暴露或其他记录边界绕过。
- 需要受信任操作员配置输入的 ReDoS/DoS 声明（例如 `sessionFilter` 或 `logging.redactPatterns` 中的灾难性正则表达式），没有信任边界绕过。
- 需要在受信任状态下预先存在本地文件系统引导的归档/安装提取声明（例如在目标目录如 skills/tools 路径下植入符号链接/硬链接别名），而不展示可以创建/控制该原语的不受信任路径。
- 依赖于替换或重写受信任宿主上已批准的可执行路径（同路径 inode/内容交换），而不展示执行该写入的不受信任路径的报告。
- 依赖于预先存在的符号链接技能/工作区文件系统状态（例如涉及 `skills/*/SKILL.md` 的符号链接链），而不展示可以创建/控制该状态的不受信任路径的报告。
- 默认本地/环回部署上缺少 HSTS 的发现。
- 针对仅测试框架、QA Lab、QE Lab、E2E 固件、基准测试装置或维护者专用调试工具的报告，当易受攻击的代码未作为支持的生产表面发布时。
- Slack webhook 签名发现，当 HTTP 模式已使用签名密钥验证。
- 此仓库 Discord 集成不使用路径的 Discord 入站 webhook 签名发现。
- 声明 Microsoft Teams `fileConsent/invoke` `uploadInfo.uploadUrl` 是攻击者控制的，而不展示以下任一：身份验证边界绕过、携带攻击者选择 URL 的真实经过身份验证的 Teams/Bot Framework 事件，或 Microsoft/Bot 信任路径的破坏。
- 仅针对过期/不存在路径的扫描仪声明，或没有工作重现的声明。
- 针对较新发布版本重申已修复问题，而不展示易受攻击路径在该较新版本的已发布标签或已发布产物中仍然存在的报告。
- 针对操作员管理的 HTTP/WebSocket 代理路由功能的 SSRF 报告，其唯一声明是普通进程本地 HTTP 客户端（`fetch`、`node:http`、`node:https`、WebSocket 客户端、axios/got/node-fetch 风格客户端）可以在代理路由禁用、缺失或操作员管理的代理策略允许时访问内部、元数据、私有或其他敏感目标。对于此功能，OpenClaw 在启用时提供故障关闭代理路由；外部代理的目标策略是操作员基础设施，不是 OpenClaw 控制的安全边界。参见[网络代理](https://docs.openclaw.ai/security/network-proxy)。

### 通过 CLI 维护者 GHSA 更新

通过 `gh api` 修补 GHSA 时，包含 `X-GitHub-Api-Version: 2022-11-28`（或更新版本）。没有它，某些字段（特别是 CVSS）可能不会持久化，即使请求返回 200。

### 操作员信任模型

OpenClaw **不**将一个网关建模为多租户、对抗性用户边界。

- 已认证的网关调用者被视为该网关实例的受信任操作员。
- 使用共享网关密钥（`token` / `password`）进行身份验证的直接本地/环回控制 UI 和网关 WebSocket 会话在同一受信任操作员桶中。该路径上的本地自动配对设备会话期望保留完整的本地主机操作员能力；它们不创建单独的 `operator.write` 对 `operator.admin` 安全边界。
- HTTP 兼容端点（`POST /v1/chat/completions`、`POST /v1/responses`）和直接工具端点（`POST /tools/invoke`）在同一受信任操作员桶中。在那里传递网关承载身份验证等同于该网关的操作员访问；它们不实现更窄的 `operator.write` 对 `operator.admin` 信任拆分。
- 具体来说，在 OpenAI 兼容 HTTP 表面上：
  - 共享密钥承载身份验证（`token` / `password`）验证对网关操作员密钥的拥有
  - 这些请求接收完整的默认操作员范围集（`operator.admin`、`operator.read`、`operator.write`、`operator.approvals`、`operator.pairing`）
  - 聊天回合端点（`/v1/chat/completions`、`/v1/responses`）也将这些共享密钥调用者视为所有者专用工具策略的所有者发送者
  - `POST /tools/invoke` 遵循相同的共享密钥规则，也将这些调用者视为所有者专用工具策略的所有者发送者
  - 对于该共享密钥路径，更窄的 `x-openclaw-scopes` 头被忽略
  - 只有承载身份 HTTP 模式（例如受信任的代理身份验证或私有入口的 `gateway.auth.mode="none"`）才尊重声明的每请求操作员范围
- 会话标识符（`sessionKey`、会话 ID、标签）是路由控制，不是每用户授权边界。
- 如果一个操作员可以查看同一网关上另一个操作员的数据，这在此信任模型中是预期的。
- OpenClaw 可以在一台机器上技术上运行多个网关实例，但推荐的操作是按信任边界清晰分离。
- 推荐模式：每台机器/宿主（或 VPS）一个用户，该用户一个网关，以及该网关内的一个或多个代理。
- 如果多个用户需要 OpenClaw，每个用户使用一个 VPS（或宿主/OS 用户边界）。
- 对于高级设置，一台机器上的多个网关是可能的，但只有在严格隔离的情况下，不是推荐的默认设置。
- Exec 行为默认为宿主优先：`agents.defaults.sandbox.mode` 默认为 `off`。
- `tools.exec.host` 默认为 `auto`：当沙盒运行时对会话活跃时沙盒，否则网关。
- 隐式 exec 调用（工具调用中没有明确宿主）遵循相同行为。
- 这在 OpenClaw 的单用户受信任操作员模型中是预期的。如果您需要隔离，启用沙盒模式（`non-main`/`all`）并保持严格的工具策略。

### 受信任插件

插件/扩展是网关信任计算基础的一部分。

- 安装或启用插件会赋予它与在该网关宿主上运行的本地代码相同的信任级别。
- 插件读取 env/文件或运行宿主命令等行为在此信任边界内是预期的。
- 安全报告必须展示边界绕过（例如未经身份验证的插件加载、允许列表/策略绕过或沙盒/路径安全绕过），而不仅仅是来自受信任安装插件的恶意行为。

### 超出范围

- 公共互联网暴露
- 以文档建议不要的方式使用 OpenClaw
- 仅测试代码和维护者框架，包括 QA Lab、QE Lab、E2E 固件、基准测试装置、冒烟测试容器和本地调试代理，除非报告证明相同的易受攻击行为可以从已发布的 OpenClaw 生产代码或面向用户的已发布包产物访问。
- 相互不信任/对抗性操作员共享一个网关宿主和配置的部署（例如，期望 `sessions.list`、`sessions.preview`、`chat.history` 或类似控制平面读取的每操作员隔离的报告）
- 仅提示注入攻击（没有策略/身份验证/沙盒边界绕过）
- 需要对受信任本地状态（`~/.openclaw`、工作区文件如 `MEMORY.md` / `memory/*.md`）的写入访问的报告
- 可利用性依赖于攻击者控制的在受信任本地路径（例如提取/安装目标树）中预先存在的符号链接/硬链接文件系统状态的报告，除非展示了创建该状态的单独不受信任边界绕过。
- 仅声明通过受信任的本地技能/工作区符号链接状态（例如 `skills/*/SKILL.md` 符号链接链）进行沙盒/工作区读取扩展的报告，除非展示了创建/控制该状态的单独不受信任边界绕过。
- 仅声明在受信任宿主上通过同路径文件替换/重写进行批准后可执行身份漂移的报告，除非展示了该宿主写入原语的单独不受信任边界绕过。
- 已授权发送者有意调用本地操作命令（例如 `/export-session` 写入绝对宿主路径）而不绕过身份验证、沙盒或其他记录边界的报告
- 仅声明使用明确受信任操作员控制表面（例如 `canvas.eval`、浏览器评估/脚本执行或直接 `node.invoke` 执行）而不展示身份验证、策略、允许列表、批准或沙盒绕过的报告。
- 仅声明受信任安装/启用的插件可以以网关/宿主权限执行的报告（已记录的信任模型行为）。
- 任何仅声明操作员启用的 `dangerous*`/`dangerously*` 配置选项削弱默认值的报告（这些是有意的"打破玻璃"权衡）
- 依赖于受信任操作员提供的配置值来触发可用性影响的报告（例如自定义正则表达式模式）。这些仍可能作为深度防御加固修复，但不是安全边界绕过。
- 仅声明命令风险检测（例如混淆模式检查）在 exec 表面上的启发式/对等漂移，而没有已证明的信任边界绕过的报告。这些是仅加固发现，不是漏洞；分类可能将其关闭为 `invalid`/`no-action` 或单独将其作为低/信息性加固跟踪。
- 仅声明 ACP 暴露的工具可以间接执行命令、修改宿主状态或访问另一个特权工具/运行时，而不展示 ACP 提示/批准、允许列表执行、沙盒或其他记录信任边界绕过的报告。这些是仅加固发现，不是漏洞。
- 仅声明 exec 批准不会语义建模每种解释器/运行时加载器形式、子命令、标志组合、包脚本或传递模块/配置导入的报告。Exec 批准绑定确切请求上下文和尽力而为的直接本地文件操作数；它们不是运行时可能加载的所有内容的完整语义模型。
- 仅声明在最新维护依赖项中的解析器可达性而不展示确切已发布依赖版本易受攻击的报告。我们保持本机媒体依赖项最新；仅依赖暴露不是漏洞。
- 仅声明来自解码/编码、base64 扩展、媒体转码、序列化或格式转换顺序的资源开销，在输入已通过适用的已配置接受限制的情况下，包括 base64 解码前大小估计发现的报告。这些是仅性能的，应被 GHSA 分类忽略，除非报告证明未经身份验证的放大、限制绕过、崩溃/进程终止、持续耗尽、数据暴露或其他记录边界绕过。
- 第三方/用户控制凭证（不是 OpenClaw 拥有的，也不授予对 OpenClaw 运营的基础设施/服务的访问权限）的暴露密钥，没有已证明的 OpenClaw 影响
- 仅声明在沙盒运行时禁用/不可用时（已记录的受信任操作员模型中的默认行为）的宿主端 exec 的报告，没有边界绕过。
- 仅声明平台提供的上传目标 URL 不受信任的报告（例如 Microsoft Teams `fileConsent/invoke` `uploadInfo.uploadUrl`），而不在经过身份验证的生产流中证明攻击者控制。
- 限于操作员管理的 HTTP/WebSocket 代理路由功能的 SSRF 报告，其中证明的缓解措施是启用/配置带有过滤 `proxy.proxyUrl`/`OPENCLAW_PROXY_URL` 的 `proxy.enabled`，或影响依赖于宽松/配置错误的操作员代理。这仅涵盖正常进程本地 HTTP(S)/WebSocket 出口（`fetch`、Node HTTP(S) 和类似 JavaScript 客户端）；非 HTTP 出口和其他功能单独评估。参见[网络代理](https://docs.openclaw.ai/security/network-proxy)。

### 部署假设

OpenClaw 安全指导假设：

- OpenClaw 运行的宿主在受信任的 OS/管理边界内。
- 任何可以修改 `~/.openclaw` 状态/配置（包括 `openclaw.json`）的人实际上是受信任的操作员。
- 由相互不信任的人共享的单个网关**不是推荐的设置**。每个信任边界使用单独的网关（或至少单独的 OS 用户/宿主）。
- 已认证的网关调用者被视为受信任操作员。会话标识符（例如 `sessionKey`）是路由控制，不是每用户授权边界。
- 多个网关实例可以在一台机器上运行，但推荐模式是清晰的每用户隔离（优先每用户一个宿主/VPS）。

### 单用户信任模型

OpenClaw 的安全模型是"个人助手"（一个受信任的操作员，可能有多个代理），不是"共享多租户总线"。

- 如果多人可以向同一个工具启用的代理发送消息（例如共享 Slack 工作区），他们都可以在其授予权限范围内引导该代理。
- 非所有者发送者状态只影响所有者专用工具/命令。如果非所有者仍然可以访问同一代理上的非所有者专用工具（例如 `canvas`），这在授予的工具边界内，除非报告展示身份验证、策略、允许列表、批准或沙盒绕过。
- 会话或内存范围减少上下文泄漏，但**不**创建每用户宿主授权边界。
- 对于混合信任或对抗性用户，按 OS 用户/宿主/网关隔离，并每个边界使用单独的凭证。
- 公司共享代理可以是有效设置，当用户在同一信任边界内且代理严格仅用于业务时。
- 对于公司共享设置，使用专用机器/VM/容器和专用账户；避免在该运行时上混合个人数据。
- 如果该宿主/浏览器配置文件登录到个人账户（例如 Apple/Google/个人密码管理器），您已经崩溃了边界并增加了个人数据暴露风险。

### 上下文可见性和允许列表

OpenClaw 区分：

- **触发授权**：谁可以触发代理（`dmPolicy`、`groupPolicy`、允许列表、提及门控）
- **上下文可见性**：向模型提供什么补充上下文（回复正文、引用文本、线程历史、转发元数据）

在当前版本中，允许列表主要门控触发和所有者风格命令访问。它们不保证跨每个频道/表面的通用补充上下文脱敏。

当前频道行为并不完全统一：

- 一些频道已经按发送者允许列表过滤部分补充上下文
- 其他频道仍然按接收传递补充上下文

仅展示补充上下文可见性差异的报告通常是加固/一致性发现，除非它们还展示了记录的边界绕过（身份验证、策略、批准、沙盒或等效）。

加固路线图可能添加明确的可见性模式（例如 `all`、`allowlist`、`allowlist_quote`），以便操作员可以以可预测的权衡选择更严格的上下文过滤。

### 代理和模型假设

- 模型/代理**不是**受信任主体。假设提示/内容注入可以操纵行为。
- 安全边界来自宿主/配置信任、身份验证、工具策略、沙盒和 exec 批准。
- 提示注入本身不是漏洞报告，除非它跨越这些边界之一。
- 钩子/webhook 驱动的有效载荷应被视为不受信任的内容；除非进行严格范围的调试，否则保持不安全绕过标志禁用（`hooks.gmail.allowUnsafeExternalContent`、`hooks.mappings[].allowUnsafeExternalContent`）。
- 弱模型层通常更容易进行提示注入。对于工具启用或钩子驱动的代理，优先使用强大的现代模型层和严格的工具策略（例如 `tools.profile: "messaging"` 或更严格），以及尽可能沙盒化。

### 网关和节点信任概念

OpenClaw 将路由与执行分离，但两者都保持在同一操作员信任边界内：

- **网关**是控制平面。如果调用者通过网关身份验证，他们被视为该网关的受信任操作员。
- **节点**是网关的执行扩展。配对节点授予该节点上的操作员级远程能力。
- **Exec 批准**（允许列表/询问 UI）是减少意外命令执行的操作员护栏，不是多租户授权边界。
- Exec 批准绑定确切命令/cwd/env 上下文，当 OpenClaw 可以识别一个具体本地脚本/文件操作数时，也绑定该文件快照。这是尽力而为的完整性加固，不是每种解释器/运行时加载器路径的完整语义模型。
- exec 表面（`gateway`、`node`、`sandbox`）之间的命令风险警告启发式差异本身不构成安全边界绕过。
- 对于不受信任用户隔离，按信任边界拆分：每个边界单独的网关和单独的 OS 用户/宿主。

### 工作区内存信任边界

`MEMORY.md` 和 `memory/*.md` 是普通工作区文件，被视为受信任的本地操作员状态。

- 如果有人可以编辑工作区内存文件，他们已经跨越了受信任操作员边界。
- 这些文件上的内存搜索索引/召回是预期行为，不是沙盒/安全边界。
- 被视为超出范围的示例报告模式："攻击者将恶意内容写入 `memory/*.md`，然后 `memory_search` 返回它。"
- 如果您需要相互不信任用户之间的隔离，按 OS 用户或宿主拆分并运行单独的网关。

### 插件信任边界

插件/扩展与网关**在进程内**加载，被视为受信任代码。

- 插件可以以与 OpenClaw 进程相同的 OS 权限执行。
- 运行时辅助函数（例如 `runtime.system.runCommandWithTimeout`）是便利 API，不是沙盒边界。
- 仅安装您信任的插件，并优先使用 `plugins.allow` 来固定明确受信任的插件 id。

### 临时文件夹边界

OpenClaw 为本地媒体交接和沙盒相邻临时产物使用专用临时根：

- 首选临时根：`/tmp/openclaw`（在宿主上可用且安全时）。
- 回退临时根：`os.tmpdir()/openclaw`（或多用户宿主上的 `openclaw-<uid>`）。

安全边界说明：

- 沙盒媒体验证仅允许 OpenClaw 管理的临时根下的绝对临时路径。
- 任意宿主临时路径不被视为受信任的媒体根。
- 插件/扩展代码在处理媒体文件时应使用 OpenClaw 临时辅助函数（`resolvePreferredOpenClawTmpDir`、`buildRandomTempFilePath`、`withTempDownloadPath`），而非原始 `os.tmpdir()` 默认值。
- 执行参考点：
  - 临时根解析器：`src/infra/tmp-openclaw-dir.ts`
  - SDK 临时辅助函数：`src/plugin-sdk/temp-path.ts`
  - 消息/频道临时护栏：`scripts/check-no-random-messaging-tmp.mjs`

### 操作指导

有关威胁模型 + 加固指导（包括 `openclaw security audit --deep` 和 `--fix`），请参见：

- `https://docs.openclaw.ai/gateway/security`

#### 工具文件系统加固

- `tools.exec.applyPatch.workspaceOnly: true`（推荐）：将 `apply_patch` 写入/删除限制在配置的工作区目录内。
- `tools.fs.workspaceOnly: true`（可选）：将 `read`/`write`/`edit`/`apply_patch` 路径和本机提示图像自动加载路径限制在工作区目录。
- 避免设置 `tools.exec.applyPatch.workspaceOnly: false`，除非您完全信任可以触发工具执行的人。

#### 子代理委托加固

- 除非明确需要委托运行，否则保持 `sessions_spawn` 被拒绝。
- 保持 `agents.list[].subagents.allowAgents` 窄，只包含您信任其沙盒设置的代理。
- 当委托必须保持沙盒化时，使用 `sandbox: "require"` 调用 `sessions_spawn`（默认为 `inherit`）。
  - `sandbox: "require"` 拒绝生成，除非目标子运行时被沙盒化。
  - 这防止限制较少的会话意外地将工作委托到未沙盒化的子级。

#### Web 接口安全

OpenClaw 的 Web 接口（网关控制 UI + HTTP 端点）仅供**本地使用**。

- 推荐：保持网关**仅环回**（`127.0.0.1` / `::1`）。
  - 配置：`gateway.bind="loopback"`（默认）。
  - CLI：`openclaw gateway run --bind loopback`。
- `gateway.controlUi.dangerouslyDisableDeviceAuth` 仅用于本地主机的"打破玻璃"使用。
  - OpenClaw 在设计上保持部署灵活性，不硬性禁止非本地设置。
  - 非本地和其他有风险的配置由 `openclaw security audit` 标记为危险发现。
  - 此操作员选择的权衡是有意的设计，本身不是安全漏洞。
- Canvas 宿主说明：网络可见的 canvas 对于受信任节点场景（LAN/tailnet）是**有意的**。
  - 预期设置：非环回绑定 + 网关身份验证（令牌/密码/受信任代理）+ 防火墙/tailnet 控制。
  - 预期路由：`/__openclaw__/canvas/`、`/__openclaw__/a2ui/`。
  - 仅此部署模型本身不是安全漏洞。
- **不要**将其暴露到公共互联网（不直接绑定到 `0.0.0.0`，不公开反向代理）。它不针对公共暴露加固。
- 如果您需要远程访问，优先使用 SSH 隧道或 Tailscale serve/funnel（因此网关仍绑定到环回），加上强大的网关身份验证。
- 网关 HTTP 表面包括 canvas 宿主（`/__openclaw__/canvas/`、`/__openclaw__/a2ui/`）。将 canvas 内容视为敏感/不受信任，避免将其暴露到环回之外，除非您理解风险。

## 运行时要求

### Node.js 版本

OpenClaw 需要 **Node.js 22.14.0 或更高版本**（LTS）。此版本包含重要的安全补丁：

- CVE-2025-59466：async_hooks DoS 漏洞
- CVE-2026-21636：权限模型绕过漏洞

验证您的 Node.js 版本：

```bash
node --version  # 应为 v22.14.0 或更高版本
```

### Docker 安全

在 Docker 中运行 OpenClaw 时：

1. 官方镜像以非 root 用户（`node`）运行，以减少攻击面
2. 尽可能使用 `--read-only` 标志以获得额外的文件系统保护
3. 使用 `--cap-drop=ALL` 限制容器能力

安全 Docker 运行示例：

```bash
docker run --read-only --cap-drop=ALL \
  -v openclaw-data:/app/data \
  openclaw/openclaw:latest
```

## 安全扫描

OpenClaw 使用多个安全和版本验证层。没有任何单一扫描器被视为边界。

### 密钥检测

OpenClaw 在 CI 中运行预提交 `detect-private-key` 钩子，并保持密钥解析行为由专用密钥测试表面覆盖。

在本地运行密钥扫描：

```bash
pre-commit run --all-files detect-private-key
```

### 静态分析

CI 使用 `.github/workflows/codeql*.yml` 和 `.github/codeql/*.yml` 跨核心 TypeScript、GitHub Actions、Android、macOS 和高风险运行时边界运行 CodeQL。

OpenGrep 提供高精度的 Semgrep 兼容层。PR 运行变更路径扫描；维护者可以在需要时运行完整仓库扫描。规则包位于 `security/opengrep/` 下，`.semgrepignore` 作为共享排除文件。

安装 `opengrep` 后运行本地 OpenGrep 封装器：

```bash
scripts/run-opengrep.sh --changed --sarif --error
pnpm check:opengrep-rule-metadata
```

### 端到端和实时验证

与安全相关的行为也由运行时验证覆盖，不仅仅是静态扫描：

- `pnpm test:e2e` 用于仓库 E2E 覆盖。
- `pnpm test:live` 用于实时提供者/运行时覆盖。
- `pnpm test:docker:all` 用于 Docker 打包运行时场景。
- 包验收和计划实时/E2E 工作流用于发布路径验证。

这些通道演练打包安装、网关/运行时行为、实时模型/提供者路径、Docker 场景和平台冒烟测试。它们通过证明安全敏感流在真实运行时环境中仍然正确行为来补充扫描仪。
