# 变更日志

## 0.2.0 — 2025-12-23

### 亮点
- 新增 `SwabbleKit`（多平台唤醒词门控工具，带有基于片段的间隙检测）。
- Swabble 包现在支持 iOS + macOS 消费者；CLI 仍然仅限 macOS 26。

### 变更
- CLI 唤醒词匹配/去除通过 `SwabbleKit` 辅助函数路由。
- 语音管道类型现在明确限制在 macOS 26 / iOS 26 可用性范围内。
