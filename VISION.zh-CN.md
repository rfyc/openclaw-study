## OpenClaw 愿景

OpenClaw 是真正能做事情的 AI。
它运行在您的设备上，在您使用的频道中，按照您的规则。

本文档说明了项目的当前状态和方向。
我们还处于早期阶段，所以迭代很快。
项目概述和开发者文档：[`README.md`](README.md)
贡献指南：[`CONTRIBUTING.md`](CONTRIBUTING.md)

OpenClaw 最初是作为学习 AI 并构建真正有用事物的个人试验场：
一个可以在真实计算机上执行真实任务的助手。
它经历了几个名字和形态的演变：Warelay -> Clawdbot -> Moltbot -> OpenClaw。

目标：一个易于使用、支持广泛平台且尊重隐私和安全的个人助手。

当前重点：

优先级：

- 安全和安全默认值
- Bug 修复和稳定性
- 设置可靠性和首次运行用户体验

下一个优先级：

- 支持所有主要模型提供商
- 改进对主要消息频道的支持（并添加一些高需求的频道）
- 性能和测试基础设施
- 更好的计算机使用和代理框架能力
- CLI 和 Web 前端的人机工程学
- macOS、iOS、Android、Windows 和 Linux 上的配套应用

贡献规则：

- 一个 PR = 一个问题/话题。不要捆绑多个不相关的修复/功能。
- 超过 ~5,000 行变更的 PR 只在特殊情况下审查。
- 不要一次打开大批量的小型 PR；每个 PR 都有审查成本。
- 对于非常小的相关修复，鼓励分组成一个有针对性的 PR。

## 安全

OpenClaw 的安全是一种深思熟虑的权衡：在不损害能力的情况下实现强大默认值。
目标是在明确操作员控制的风险路径下，为真实工作保持强大能力。

规范安全策略和报告：

- [`SECURITY.md`](SECURITY.md)

我们优先考虑安全默认值，但也为受信任的高功率工作流暴露清晰的控制。

## 插件与内存

OpenClaw 有一个广泛的插件 API。
核心保持精简；可选功能通常应作为插件发布。
我们总体上在精简核心的同时扩展插件可以做的事情。
如果一个有用的功能还不能作为插件构建，我们欢迎扩展插件 API 而不是添加一次性核心行为的 PR 和设计讨论。

有两种广泛的插件风格：

- 代码插件运行 OpenClaw 插件代码，适用于更深层次的运行时扩展。
- 捆绑式插件打包稳定的外部表面，如技能、MCP 服务器和相关配置。

当捆绑式插件可以表达该能力时，优先使用捆绑式插件。
它们有更小、更稳定的接口和更好的安全边界。
当能力需要运行时钩子、提供者、频道、工具或其他进程内扩展点时，使用代码插件。

首选插件路径是 npm 包分发加上本地扩展加载用于开发。
如果您构建了一个插件，在您自己的仓库中托管和维护它。
在核心中添加可选插件的门槛故意很高。
插件文档：[`docs/tools/plugin.md`](docs/tools/plugin.md)
插件发现、官方发布者状态、来源和安全审查在 [ClawHub](https://clawhub.ai/) 中。
OpenClaw 文档应记录核心扩展点；插件推广属于 ClawHub，官方插件最好在经过审查的组织发布者下。

内存是一个特殊的插件槽，一次只能有一个内存插件处于活动状态。
今天我们发布了多个内存选项；随着时间推移，我们计划收敛到一个推荐的默认路径。

### 技能

我们仍然为基础用户体验发布一些捆绑技能。
新技能应首先通过 [ClawHub](https://clawhub.ai/) 发布，而不是默认添加到核心。
官方或捆绑推广应需要明确的产品、安全或维护者所有权原因。

### MCP 支持

OpenClaw 将 MCP 支持为服务器和运行时集成表面。
MCP 详情在 [`docs/cli/mcp.md`](docs/cli/mcp.md) 中。

项目目标是实用的 MCP 支持，而不重复现有的代理、工具、ACPX、插件或 ClawHub 路径。

### 设置

OpenClaw 目前设计为终端优先。
这使设置明确：用户预先看到文档、身份验证、权限和安全姿态。

长期来看，我们希望随着加固成熟而提供更简单的入门流程。
我们不希望隐藏关键安全决策的便利包装器。

### 为什么使用 TypeScript？

OpenClaw 主要是一个编排系统：提示、工具、协议和集成。
选择 TypeScript 是为了保持 OpenClaw 默认可黑客。
它被广泛了解，迭代快速，易于阅读、修改和扩展。

## 我们暂时不会合并的内容

- 当可以在 [ClawHub](https://clawhub.ai/) 上生存的新核心技能
- 所有文档的完整文档翻译集（推迟；我们计划稍后进行 AI 生成翻译）
- 明显不符合模型提供者类别的商业服务集成
- 没有明确能力或安全差距的已支持频道的包装器频道
- 没有明确产品或安全差距就重复现有 MCP、ACPX、插件或 ClawHub 路径的 MCP 工作
- 代理层次框架（管理者的管理者/嵌套规划器树）作为默认架构
- 重复现有代理和工具基础设施的重型编排层

此列表是路线图护栏，不是物理定律。
强大的用户需求和强大的技术理由可以改变它。
