## 摘要

用 2-5 个要点描述问题和修复：

如果此 PR 修复了插件 beta 发布阻塞问题，标题格式为 `fix(<plugin-id>): beta blocker - <summary>`，并链接相应的 `Beta blocker: <plugin-name> - <summary>` issue（标记为 `beta-blocker`）。贡献者无法为 PR 添加标签，因此标题是维护者和自动化的 PR 端信号。

- 问题：
- 为什么重要：
- 变更内容：
- 未变更内容（范围边界）：

## 变更类型（选择所有适用项）

- [ ] Bug 修复
- [ ] 新功能
- [ ] 修复所需的重构
- [ ] 文档
- [ ] 安全加固
- [ ] 杂项/基础设施

## 范围（选择所有触及的区域）

- [ ] 网关/编排
- [ ] 技能/工具执行
- [ ] 身份验证/令牌
- [ ] 内存/存储
- [ ] 集成
- [ ] API/合约
- [ ] UI/开发体验
- [ ] CI/CD/基础设施

## 关联 Issue/PR

- 关闭 #
- 相关 #
- [ ] 此 PR 修复了一个 bug 或回归问题

## 根本原因（如适用）

对于 bug 修复或回归，解释为什么会发生，而不仅仅是什么发生了变化。否则写 `N/A`。如果原因不清楚，写 `Unknown`（未知）。

- 根本原因：
- 缺少的检测/防护：
- 已知的相关背景（如有）：

## 回归测试计划（如适用）

对于 bug 修复或回归，命名应该捕获此问题的最小可靠测试覆盖。否则写 `N/A`。

- 本应捕获此问题的覆盖级别：
  - [ ] 单元测试
  - [ ] 接缝/集成测试
  - [ ] 端到端测试
  - [ ] 现有覆盖已足够
- 目标测试或文件：
- 测试应锁定的场景：
- 为什么这是最小可靠的防护：
- 已覆盖此问题的现有测试（如有）：
- 如果没有添加新测试，原因是：

## 用户可见/行为变更

列出用户可见的变更（包括默认值/配置）。
如果没有，写 `无`。

## 图表（如适用）

对于 UI 变更或非平凡的逻辑流程，包含审阅者可以快速浏览的小型 ASCII 图表。否则写 `N/A`。

```text
之前：
[用户操作] -> [旧状态]

之后：
[用户操作] -> [新状态] -> [结果]
```

## 安全影响（必填）

- 新权限/功能？（是/否）
- 密钥/令牌处理有变更？（是/否）
- 新增/变更网络调用？（是/否）
- 命令/工具执行表面有变更？（是/否）
- 数据访问范围有变更？（是/否）
- 如果任何一项为"是"，解释风险 + 缓解措施：

## 复现 + 验证

### 环境

- 操作系统：
- 运行时/容器：
- 模型/提供者：
- 集成/频道（如有）：
- 相关配置（已脱敏）：

### 步骤

1.
2.
3.

### 预期结果

-

### 实际结果

-

## 证据

至少附上以下之一：

- [ ] 修复前的失败测试/日志 + 修复后通过
- [ ] 追踪/日志片段
- [ ] 截图/录屏
- [ ] 性能数据（如相关）

## 人工验证（必填）

您亲自验证的内容（不仅仅是 CI），以及如何验证：

- 已验证的场景：
- 已检查的边界情况：
- 您**未**验证的内容：

## 审查对话

- [ ] 我回复或解决了此 PR 中处理的每个机器人审查对话。
- [ ] 我只保留了仍需要审阅者或维护者判断的未解决对话。

如果机器人审查对话通过此 PR 得到处理，请自行解决该对话。不要将机器人审查对话清理工作留给维护者。

## 兼容性/迁移

- 向后兼容？（是/否）
- 配置/环境变更？（是/否）
- 需要迁移？（是/否）
- 如果是，确切的升级步骤：

## 风险与缓解措施

仅列出此 PR 的真实风险。根据需要添加/删除条目。如果没有，写 `无`。

- 风险：
  - 缓解措施：
