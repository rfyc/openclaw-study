---
summary: "重定向：流命令位于 `openclaw tasks flow` 下"
read_when:
  - 你在旧版文档或发行说明中遇到 openclaw flows
title: "Flows（重定向）"
---

# `openclaw tasks flow`

流命令是 `openclaw tasks` 的子命令，而不是独立的 `flows` 命令。

```bash
openclaw tasks flow list [--json]
openclaw tasks flow show <lookup>
openclaw tasks flow cancel <lookup>
```

完整文档请参阅 [Task Flow](/automation/taskflow) 和 [tasks CLI 参考](/cli/tasks)。

## 相关

- [CLI 参考](/cli)
- [Automation](/automation)
